server:
  port: 8080

spring:
  application:
    name: api-monitoring-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

opensearch:
  host: localhost
  port: 9200
  scheme: https
  username: admin
  password: Str0ng@ApiMon#2025   # from docker-compose.yml
