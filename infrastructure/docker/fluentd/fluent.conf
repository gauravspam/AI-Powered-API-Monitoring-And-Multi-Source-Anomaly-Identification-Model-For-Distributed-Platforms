<source>
  @type http
  bind 0.0.0.0
  port 9880
</source>

<match api-logs.*>
  @type opensearch
  host opensearch
  port 9200
  scheme http
  logstash_format true
  logstash_prefix api-logs
  suppress_type_name true
</match>

<match *>
  @type stdout
</match>
